# Real third-party dependencies only, pinned.
# The data cross-check layer (src/profile/) and the heating pipeline
# (src/heating/) stream with the stdlib; pandas/numpy et al. get added only
# if they ever measurably need them.

jsonschema==4.26.0
pytest==9.1.1

# Knowledge-graph emitter (src/brick/): Brick Schema graphs as Turtle.
rdflib==7.6.0

# Only needed for the side tools in src/PDF-parser/ (not the decode pipeline).
pymupdf==1.27.2.3

# Neo4j import (src/heating/neo4j_import.py)
neo4j==6.2.0

# Only for the r/y/u probe's plots (src/heating/ryu_probe.py), lazily imported.
matplotlib==3.11.0
